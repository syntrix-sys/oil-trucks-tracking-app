{
  "name": "mock-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "start": "node dist/src/index.js",
    "dev": "ts-node src/index.ts",
    "dev:fast": "cross-env TICK_INTERVAL_MS=200 ts-node src/index.ts",
    "generate": "ts-node scripts/generate.ts",
    "start:fast": "cross-env TICK_INTERVAL_MS=200 ts-node src/index.ts"
  },
  "dependencies": {
    "@oiltrack/types": "*",
    "dotenv": "^17.4.2",
    "seedrandom": "^3.0.5",
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "@types/seedrandom": "^3.0.8",
    "@types/ws": "^8.5.10",
    "cross-env": "^7.0.3",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
